/* 
 *	ctrlDefault.js : This File handles default route of the application's boot.
 */
angularControllers.controller('ctrlDefault', ['$scope', function($scope) {

    $scope.init = function() {
    	// Default Page Action or inappropriate Route Redirection actions can be handled here
    };

    $scope.init();

}]);